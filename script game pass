local Players = game:GetService("Players")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

local LINK_GAMEPASS = "https://www.roblox.com/pt/game-pass/1674607640/100"

if setclipboard then setclipboard(LINK_GAMEPASS) end

if playerGui:FindFirstChild("TelaBloqueioPro") then
    playerGui.TelaBloqueioPro:Destroy()
end

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "TelaBloqueioPro"
screenGui.IgnoreGuiInset = true
screenGui.Parent = playerGui

local frame = Instance.new("Frame")
frame.Size = UDim2.new(1, 0, 1, 0)
frame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
frame.Active = true
frame.Parent = screenGui

local layout = Instance.new("UIListLayout")
layout.Padding = UDim.new(0, 20)
layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
layout.VerticalAlignment = Enum.VerticalAlignment.Center
layout.Parent = frame

local txtPT = Instance.new("TextLabel")
txtPT.Size = UDim2.new(0.8, 0, 0.3, 0)
txtPT.BackgroundTransparency = 1
txtPT.Text = "UM LINK FOI COPIADO! COLE NO SEU NAVEGADOR E COMPRE A GAME PASS PARA LIBERAR O SCRIPT."
txtPT.TextColor3 = Color3.new(1, 1, 1)
txtPT.TextScaled = true
txtPT.Font = Enum.Font.GothamBold
txtPT.Parent = frame

local linha = Instance.new("Frame")
linha.Size = UDim2.new(0.5, 0, 0, 2)
linha.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
linha.Parent = frame

local txtES = Instance.new("TextLabel")
txtES.Size = UDim2.new(0.8, 0, 0.25, 0)
txtES.BackgroundTransparency = 1
txtES.Text = "¡UN LINK FUE COPIADO! PÉGALO EN TU NAVEGADOR Y COMPRA EL GAME PASS PARA DESBLOQUEAR O SCRIPT."
txtES.TextColor3 = Color3.fromRGB(200, 200, 200)
txtES.TextScaled = true
txtES.Font = Enum.Font.GothamMedium
txtES.Parent = frame
